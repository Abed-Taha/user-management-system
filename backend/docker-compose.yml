services:
  db:
    image: postgres:17
    container_name: user-management-db
    environment:
      POSTGRES_USER: devUser
      POSTGRES_PASSWORD: devUser123
      POSTGRES_DB: user_management
    ports:
      - '5432:5432'
